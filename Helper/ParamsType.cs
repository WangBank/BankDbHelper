namespace BankDbHelper
{
    public enum ParamsType
    {
        Varchar,

        Int,

        DateTime,

        Decimal,

        Blob,
        /// <summary>
        /// 用于Oracle
        /// </summary>
        Varchar2
    }
}